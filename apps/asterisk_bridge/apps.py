from django.apps import AppConfig

class AsteriskBridgeConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.asterisk_bridge'
    verbose_name = 'Asterisk Bridge'
